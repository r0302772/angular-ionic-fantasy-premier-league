/**
 * Represents a statistic for an element (player) in the Fantasy Premier League.
 */
export interface ElementStat {
  /**
   * The label for the statistic.
   */
  label: string;
  /**
   * The name of the statistic.
   */
  name: string;
}
